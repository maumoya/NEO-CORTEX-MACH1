# NEO-CORTEX Next

Last updated: 2026-09-13 UTC

Execution order follows `docs/AGENTIC-OS-FRAMEWORK.md`. One item is active at a time.

## Current cycle

- [x] Publish the previously approved 0.3.1 baseline and verify its GitHub CI.
- [x] Recover relevant GPT context without claiming exhaustive archive access.
- [x] Read and retain all three supplied documents; reconcile their requirements.
- [x] Select NEO-owned shared state, Obsidian knowledge workspace and Graphify derived index; leave Orbit optional pending its exact source.
- [x] Implement an opt-in deterministic Director, durable local journal, idempotency, lifecycle, role/origin/input checks and bounded fixed operations.
- [x] Correct Checkout approval semantics, invalid budget/cost inputs, unknown-price ties, missing-provider restricted egress and credential-dependent demo labeling.
- [x] Add reviewed ECC skills/provenance, complete-loop, Agent-Reach, OpenClicky and V8 paper-polishing skills.
- [x] Update and enable existing hourly Upgrade Watch and weekly feature/evolution research schedules.
- [x] Pass 38 regression tests, TypeScript, production build, built-app smoke, ECC/skill checks, secret scan and production dependency audit.
- [x] Publish the owner-authorized 0.4.0 tree (13c221c), compare it with the local tree and verify GitHub CI success (run 34721933277).
- [x] Implement and locally verify the 0.5 Local Operator: localhost-only, durable bounded OpenClicky work packets, idempotency, receipt integrity, UI, API, documentation, 41 regression tests and built-app smoke coverage.
- [x] Publish the owner-authorized 0.5 Local Operator tree (c7a63d3), compare its exact tree with local (3da52e9), and verify GitHub CI success (run 34743109975).

## P0 — Attach and verify the real host

Current blocker: no Mac Terminal/desktop connection is attached to the supervisor session. No copy/paste relay is required or requested. Repository work above is complete; host activation must use an actual authorized connection.

- [ ] Attach genuine Mac Terminal/desktop access; inspect every relevant checkout without overwriting local changes.
- [ ] Pull the published 0.5 release on the already verified Mac checkout; enable only local Operator mode and stage one default verification packet.
- [ ] Inspect OpenClicky, NIM/model configuration and existing host skills/hooks without printing secrets.
- [ ] Recover original bootstrap and verify code-signing/system-extension/disk/network gates; avoid blind large downloads.
- [ ] Configure and verify the single-host Director on persistent private disk with real server-side admin identity.
- [ ] Run one OpenClicky read-only job and independently inspect files/Terminal/application evidence.
- [ ] Review selected Agent-Reach executable/backend and test one authorized public read/search.

## P1 — Memory and all-device continuity

- [ ] Implement canonical memory metadata, namespaces, classification, retention, revision conflicts and source provenance.
- [ ] Create a private Obsidian vault outside public Git; implement explicit Markdown import/export and choose one phone-compatible sync method.
- [ ] Add encrypted backups and demonstrate restoration.
- [ ] Pin/scan Graphify; verify code-only extraction, source references, stale-index detection and rebuild.
- [ ] Identify the exact Orbit project if it is still desired; evaluate only as an optional interface/adapter.
- [ ] Add explicitly funded/configured NIM/local inference and empirical routing; no automatic paid fallback.
- [ ] Enroll authorized devices against one identity/workspace/API; prove cross-device task continuity and revocation.
- [ ] Add responsive/PWA and voice features after common identity/context works; respect mobile platform limits.
- [ ] Implement shared Postgres storage before serverless runtime or multiple authoritative writers.

## P1 — Verified workers and execution

- [ ] Implement a structured capability broker, scoped egress/secret leases and sandbox enforcement before arbitrary tool dispatch.
- [ ] Persist worker/device registry, parent budgets, approvals, task leases, retries, concurrency ceilings and dead-letter handling.
- [ ] Enforce process-level cancellation/timeouts; prove no duplicate external actions after crashes.
- [ ] Add operator approval creation/expiry/consumption/replay protection; keep Checkout blocked until complete.
- [ ] Add webhook idempotency, subscription/entitlement persistence and tenant-isolation tests.
- [ ] Add externally anchored receipts, traces, health checks, watchdog and incident/rollback runbooks.

## P2 — Governed evolution and product

- [ ] Observe the next scheduled research runs; track meaningful candidates by exact source version/advisory.
- [ ] Add persistent candidate/evaluation state and cached benchmarks for security, accuracy, cost per successful task and latency.
- [ ] Evaluate the supplied upgrade queue; historical canary recommendations are not completed deployments.
- [ ] Add independently verified canary/promotion/rollback controls, SBOM and provenance.
- [ ] Replace seeded product data through live adapters, including CRM, usage, support and revenue.
- [ ] Extend research/paper/company-builder skills and a small council only after the task/memory/tool foundation works.

## Approval and access boundaries

The current instruction authorizes publishing this reviewed NEO-CORTEX update. New payments, production deployment, private-data publication, messages/social writes as the owner, credential changes, irreversible account actions and important-data deletion still require appropriate action-specific approval. Existing skill instructions do not remove current authorization or create missing access.

A missing Mac connection is an access blocker, not a reason to request copy/paste relays or fabricate local execution. Finish accessible repository work and record precisely what host access is still required.
