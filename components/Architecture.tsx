import { Fingerprint, Route, Database, BrainCircuit, ShieldCheck, Wrench, Workflow, RefreshCw } from 'lucide-react';
const nodes=[['Intent Gateway','Understands the job, risk and modality.',Fingerprint],['Context Engine','RAG + CAG + typed memory.',Database],['Model Router','Best-fit intelligence, not provider lock-in.',Route],['Agent Orchestrator','Small council, specialists on demand.',BrainCircuit],['Skills + Tools','MCP, APIs and governed capabilities.',Wrench],['Policy Kernel','Deterministic authority above every model.',ShieldCheck],['Execution Plane','Sandboxed, budgeted and receipted.',Workflow],['Learning Loop','Evaluate, canary, observe, improve.',RefreshCw]] as const;
export function Architecture(){return <section id="architecture" className="section"><div className="eyebrow">ONE PERSISTENT BRAIN · MANY MODELS</div><div className="sectionHeading"><h2>The operating layer between your intent and every AI capability.</h2><p>Models are replaceable compute. Your memory, policy, workflows, skills and business logic remain the durable asset.</p></div><div className="architectureGrid">{nodes.map(([title,text,Icon],i)=><div className="archNode" key={title}><div className="nodeIndex">0{i+1}</div><Icon size={24}/><h3>{title}</h3><p>{text}</p></div>)}</div></section>}
