import { Nav } from '@/components/Brand';import { CheckoutButton } from '@/components/CheckoutButton';import { plans } from '@/src/product/plans';export const metadata={title:'Pricing'};export default function Pricing(){return <><Nav/><main className="page"><div className="pageTitle"><div className="eyebrow">PLANS</div><h1>Buy capacity, memory and governance.</h1><p>Your subscription scales the operating system around you while model providers remain interchangeable.</p></div><div className="pricingGrid">{plans.map(p=><div className={`priceCard ${p.popular?'popular':''}`} key={p.id}>{p.popular&&<span className="badge">MOST POPULAR</span>}<h3>{p.name}</h3><div className="price">{p.price===null?'Custom':`$${p.price}`} {p.price!==null&&<small>/ month</small>}</div><p>{p.description}</p><ul>{p.features.map(f=><li key={f}>{f}</li>)}</ul>{p.id==='enterprise'?<a className="priceButton" href="mailto:sales@neo-cortex.ai">Talk to sales</a>:<CheckoutButton plan={p.id}/>}</div>)}</div><div className="statusBanner" style={{marginTop:28}}>Payments are handled server-side through Stripe when production billing keys and price IDs are configured.</div></main></>}
