import { Nav } from '@/components/Brand';import { releases } from '@/src/product/releases';export const metadata={title:'Updates'};export default function Updates(){return <><Nav/><main className="page"><div className="pageTitle"><div className="eyebrow">RELEASE INTELLIGENCE</div><h1>What changed, and why it matters.</h1><p>Every shipped release is translated from repository changes into a customer-readable product update.</p></div><div className="updates">{releases.map(r=><article className="release" key={r.version}><div className="releaseVersion">v{r.version}</div><div><h3>{r.title}</h3><p>{r.summary}</p><ul>{r.changes.map(c=><li key={c}>{c}</li>)}</ul></div><time>{r.date}</time></article>)}</div></main></>}
