import Stripe from 'stripe';export async function POST(request:Request){const secret=process.env.STRIPE_SECRET_KEY;const webhook=process.env.STRIPE_WEBHOOK_SECRET;const sig=request.headers.get('stripe-signature');if(!secret||!webhook||!sig)return Response.json({error:'Webhook not configured.'},{status:503});const stripe=new Stripe(secret);try{const body=await request.text();const event=stripe.webhooks.constructEvent(body,sig,webhook);return Response.json({received:true,type:event.type});}catch{return Response.json({error:'Invalid webhook signature.'},{status:400});}}
