import Link from 'next/link';import { SignIn } from '@clerk/nextjs';import { Nav } from '@/components/Brand';import { clerkConfigured } from '@/src/auth/config';export const metadata={title:'Sign in'};export default function SignInPage(){const configured=clerkConfigured();return <><Nav/><main className="page">{configured?<div style={{display:'grid',placeItems:'center',marginTop:70}}><SignIn routing="path" path="/sign-in" signUpUrl="/sign-in" forceRedirectUrl="/dashboard"/></div>:<div className="demoLogin"><div className="eyebrow">SECURE ACCESS</div><h1>Authentication integration ready</h1><p>This deployment is in demo mode. Clerk is wired as the production identity boundary; once its server-side keys are configured, customer and backoffice pages enforce authenticated access.</p><Link className="solidButton" href="/dashboard">Enter demo Mission Control</Link></div>}</main></>}
