export interface Checkpoint<TState = unknown> { checkpointId: string; taskId: string; correlationId: string; sequence: number; createdAt: string; state: TState; }
export interface CheckpointStore { save<TState>(checkpoint: Checkpoint<TState>): Promise<void>; latest<TState>(taskId: string): Promise<Checkpoint<TState> | null>; }
export class InMemoryCheckpointStore implements CheckpointStore { private readonly byTask = new Map<string, Checkpoint>(); async save<TState>(checkpoint: Checkpoint<TState>): Promise<void> { const current = this.byTask.get(checkpoint.taskId); if (current && current.sequence >= checkpoint.sequence) throw new Error('Checkpoint sequence must increase monotonically'); this.byTask.set(checkpoint.taskId, checkpoint as Checkpoint); } async latest<TState>(taskId: string): Promise<Checkpoint<TState> | null> { return (this.byTask.get(taskId) as Checkpoint<TState> | undefined) ?? null; } }
