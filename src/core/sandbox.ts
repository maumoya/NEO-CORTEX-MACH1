export interface SandboxNetworkPolicy { outbound: 'deny'|'allowlist'; allowedHosts: string[]; }
export interface SandboxExecutionRequest { candidateId: string; command: string; args: string[]; timeoutMs: number; network: SandboxNetworkPolicy; environmentKeys: string[]; disposableFilesystem: true; }
export interface SandboxExecutionResult { candidateId: string; exitCode: number|null; timedOut: boolean; stdoutDigest: string; stderrDigest: string; networkDestinations: string[]; fileChanges: string[]; processTree: string[]; }
export function validateSandboxRequest(request: SandboxExecutionRequest): void { if (request.network.outbound === 'deny' && request.network.allowedHosts.length > 0) throw new Error('Denied network policy cannot contain allowed hosts'); if (request.timeoutMs <= 0 || request.timeoutMs > 10*60_000) throw new Error('Sandbox timeout must be between 1ms and 10 minutes'); if (request.environmentKeys.some((key) => /secret|token|password|key/i.test(key))) throw new Error('Untrusted sandbox must not receive secret-like environment variables'); }
